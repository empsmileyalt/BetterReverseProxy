events {
    worker_connections 1024;
}

stream {
    upstream eagler_backend {
        server 89.117.77.192:25638;
    }

    server {
        listen 80;
        
        proxy_pass eagler_backend;
        
        proxy_timeout 10m;
        proxy_connect_timeout 5s;
    }
}
